import wollok.game.*
import juego.*
import personajes.*
import menu.*

// !!!!!!!!! FALTA: MUSICA EN PANTALLA DE INICIO
program JuegoSobreviviente {
	game.title("La gran aventura del Carpincho")
	game.cellSize(30)
	game.width(24)
    game.height(20)
    game.boardGround("fondo.png")
    game.start()
	menu.mostrarMenu()
}  
